# homebrew-tap

Personal Homebrew tap for Steve Johnson's projects.

## Formulae

### autowt

Install the `autowt` 0.6.0-rc5 prerelease:

```sh
brew tap irskep/tap
brew install autowt
```

The formula installs both `autowt` and `awt`.
